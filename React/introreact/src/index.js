import React from 'react';
import ReactDOM from 'react-dom';
import Componente from './app';
ReactDOM.render(
  <React.StrictMode>
    <Componente/>
  </React.StrictMode>,
  document.getElementById('root')
);
